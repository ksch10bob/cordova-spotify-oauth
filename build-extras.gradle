android {
    defaultConfig {
        minSdkVersion 25
        targetSdkVersion 31
        manifestPlaceholders = [redirectSchemeName: "yourscheme", redirectHostName: "yourhost"]
    }

    packagingOptions {
        exclude 'META-INF/NOTICE'
        exclude 'META-INF/LICENSE'
    }
}
